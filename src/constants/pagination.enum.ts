const default_count = 10;
const default_sort_order = 'ASC';

export enum user_status {
    ACTIVED = 1,    // 1 means user is active
    INACTIVED=0     // 0 means user is inactive
    
  }

export { default_count, default_sort_order };
